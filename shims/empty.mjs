/*
 * Empty shim for Node-only modules that prismarine-viewer's utils.js
 * safeRequire()s but never uses in the browser (e.g. node-canvas-webgl/lib).
 */
export const loadImage = undefined
export default {}
