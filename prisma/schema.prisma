generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./bots.db"
}

model Bot {
  id    Int    @id @default(autoincrement())
  url   String
  token String
}
